describe(
    "example",
    function() {
        beforeEach(
            function() {

            }
        );

        afterEach(
            function() {

            }
        );

        it(
            "should fail",
            function() {
                expect(true).to.be.false;
            }
        );

        it(
            "should pass",
            function() {
                expect(true).to.be.true;
            }
        );
    }
);
